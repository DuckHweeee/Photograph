import SwiftUI

/// Design tokens from PROJECT_SPEC.md §6 (light mode only for the spike).
enum Palette {
    static let cream = Color(red: 0xFB / 255, green: 0xF6 / 255, blue: 0xF0 / 255)
    static let cocoa = Color(red: 0x3D / 255, green: 0x2C / 255, blue: 0x2E / 255)
    static let secondaryText = Color(red: 0x8A / 255, green: 0x7A / 255, blue: 0x76 / 255)
    static let terracotta = Color(red: 0xE0 / 255, green: 0x7A / 255, blue: 0x5F / 255)
    static let blush = Color(red: 0xF4 / 255, green: 0xD6 / 255, blue: 0xCC / 255)
}
